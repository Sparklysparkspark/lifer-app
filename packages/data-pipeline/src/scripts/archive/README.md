One-time backfill/fix/cleanup scripts that already ran to completion against production data. Kept for reference rather than deleted outright, but none of these are meant to be run again. Not wired into any package.json script or imported by anything live.

Excluded from the TypeScript project (see ../../../tsconfig.json) and NOT runnable as-is: their relative imports resolved correctly from `src/scripts/`, not from this one-level-deeper folder. Frozen reference material, not working code.
